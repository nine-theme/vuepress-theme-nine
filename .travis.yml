language: node_js
node_js:
  - "8"

branches:
  only:
    - master

before_install:
  - npm install -g vuepress

install:
  - rm -rm node_modules
  - npm install

before_deploy:
  - npm run build

deploy:
  provider: npm
  email: "wangjia_1919@163.com"
  skip_cleanup: true
  api_key: ${NPM_TOKEN}
  on:
    branch: master
    tags: true
  tag: next
